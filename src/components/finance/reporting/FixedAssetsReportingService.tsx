import { ServiceBootstrap } from "../shared/ServiceBootstrap";

export function FixedAssetsReportingService() {
  return (
    <ServiceBootstrap
      title="Fixed Assets Reporting Service"
      category="Reporting"
      description="Asset register, depreciation schedule, and asset audit reports."
    />
  );
}
