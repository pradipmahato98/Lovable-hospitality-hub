import { ServiceBootstrap } from "../shared/ServiceBootstrap";

export function ARReportingService() {
  return (
    <ServiceBootstrap
      title="AR Reporting Service"
      category="Reporting"
      description="AR Aging, customer statements, and outstanding balances."
    />
  );
}
