import { ServiceBootstrap } from "../shared/ServiceBootstrap";

export function ConsolidationBIService() {
  return (
    <ServiceBootstrap
      title="Consolidation & BI Service"
      category="Reporting"
      description="Multi-property consolidation and hospitality KPIs (RevPAR, ADR, OCC)."
    />
  );
}
