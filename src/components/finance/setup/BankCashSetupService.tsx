import { ServiceBootstrap } from "../shared/ServiceBootstrap";

export function BankCashSetupService() {
  return (
    <ServiceBootstrap
      title="Bank & Cash Setup Service"
      category="Setup"
      description="Manage bank accounts, cash registers, and mandate/signatory rules."
    />
  );
}
