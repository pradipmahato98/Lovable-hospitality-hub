import { ServiceBootstrap } from "../shared/ServiceBootstrap";

export function AssetOperationsService() {
  return (
    <ServiceBootstrap
      title="Asset Operations Service"
      category="Transactions"
      description="Manage capitalization, transfers, revaluation, and depreciation runs."
    />
  );
}
